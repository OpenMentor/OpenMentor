<div class="container">
  <div class="form-container confirm">
    <a href="/"><img class="form-logo" src="/assets/om-mountain.png" src="Open Mentor Logo" /></a>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="actions">
        <%= f.submit "Send me reset password instructions", :class => "fill" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
