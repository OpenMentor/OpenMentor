<%= render "layouts/header" %>

<div class="container">
  <h2 class="center">Availability for <%= @mentor.name %></h2>
  <%= render partial: "shared/availabilities_index", locals: { availabilities: @availabilities, hour_range: [*0..23] } %>
  <div class="wide-button">
    <p><%= link_to "Cancel", :back, :class => "button" %></p>
  </div>
</div>
