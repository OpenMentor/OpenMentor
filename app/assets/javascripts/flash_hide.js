// Generated by CoffeeScript 1.7.1
(function() {
  var clearNotice, ready;

  clearNotice = function() {
    var lastUriSegment, uri;
    uri = window.location.href;
    lastUriSegment = uri.substr(uri.lastIndexOf("/") + 1);
    if (lastUriSegment === "sign_in" || lastUriSegment === "sign_up") {

    } else {
      $('.alert').animate({
        opacity: '0'
      }, 3000).slideUp(250);
      return $('.notice').animate({
        opacity: '0'
      }, 3000).slideUp(250);
    }
  };

  ready = function() {
    return setTimeout(clearNotice, 2000);
  };

  $(function() {
    $(document).ready(ready);
    return $(document).on('page:load', ready);
  });

}).call(this);
