// Generated by CoffeeScript 1.7.1
(function() {
  var cardFlip, clickListener;

  clickListener = function(card) {
    return card.addEventListener("click", function(e) {
      var cardClassList;
      cardClassList = $(this).closest('.effect__click')[0].classList;
      if (cardClassList.contains("flipped") === true) {
        return cardClassList.remove("flipped");
      } else {
        return cardClassList.add("flipped");
      }
    });
  };

  cardFlip = function() {
    var card, cards, _i, _len, _results;
    cards = $(".social-links");
    _results = [];
    for (_i = 0, _len = cards.length; _i < _len; _i++) {
      card = cards[_i];
      _results.push(clickListener(card));
    }
    return _results;
  };

  $(function() {
    $(document).ready(cardFlip);
    return $(document).on('page:load', cardFlip);
  });

}).call(this);
